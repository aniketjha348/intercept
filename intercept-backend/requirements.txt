# Pinned: every deploy installs EXACTLY these (unpinned >= once pulled a
# breaking major mid-release — never again). Bump deliberately + run tests.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-multipart==0.0.32
python-dotenv==1.0.1
httpx==0.28.1
websockets==17.1
pillow==12.3.0
sqlalchemy==2.0.52
psycopg[binary]==3.3.5

# Optional, keep commented until needed:
# langgraph          # stateful guardian workflow (auto-used if installed)
# langchain-core     # supporting LLM plumbing (optional, not architectural center)
# openai             # LLM provider: OpenAI
# google-generativeai # LLM provider: Gemini (we use plain HTTPS instead)
# pytesseract        # OCR (falls back to supplied text if missing)
# pyzbar             # QR decode (falls back to supplied qr_text if missing)
